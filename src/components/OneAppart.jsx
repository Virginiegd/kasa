function OneAppart(props) {
    return (
        <section>
            <div className='container'>
            <h1>{props.appartId.title}</h1>
            </div>
        </section> 

        )
}

export default OneAppart